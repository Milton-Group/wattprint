{
  "name": "@wattprint/agent-rules",
  "version": "0.1.0",
  "description": "Green-web coding rules for AI coding agents: a framework-agnostic AGENT.md plus framework supplements, and an installer that wires them into a host repo.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "pack"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  }
}
